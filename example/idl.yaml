#导入目录 支持环境变量, protoc -I 参数
includes:
  - $IDLPATH/example/idl

#当前项目依赖的proto文件
depends:
  - usermgt/passport/passport.proto

#编译时需要排除的文件或者目录
excludes:



#编译设置
generate:
    go_options:
      extra_modifiers:
        usermgt/user.proto: code.hyfco.com/usermgt/output/usermgt/userbase
        usermgt/code.proto: code.hyfco.com/usermgt/output/usermgt/userbase
    plugins:
      - name: gofast
        flags: plugins=grpc
        output: ./output
